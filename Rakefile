task :files do
  files  = Dir["lib/**/*"]
  files += Dir["bin/**/*"]
  files += Dir["config/**/*"]
  files += Dir["handlers/**/*"]
  files += Dir["script/**/*"]
  puts files.inspect
end

task :tests do
  files = Dir["spec/**/*"]
  puts files.inspect
end